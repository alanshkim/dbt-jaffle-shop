macros:
  - name: cents_to_dollars
    description: A macro that converts dollars to cents and rounds to the user input decimal places (default = 2).
    arguments:
      - name: unit
        type: string
        description: The unit column to convert.
      - name: decimals
        type: integer
        description: Number of decimal places (default = 2).